{% import '::macro/links.html.twig' as links %}

<div class="table-responsive">
    <table class="table table-striped table-bordered table-hover table-heading">
        <caption>
            <div class="pull-right">
                {{ links.pdf_link({}, {'class':'fa-lg'}) }}
            </div>
        </caption>
        <thead>
        <tr>
            <th>#</th>
            <th>Date</th>
            <th>Motif</th>
            <th>Origine</th>
            <th>Nom & Prénoms</th>
        </tr>
        </thead>
        {% if notifications is defined %}
            {% if notifications is not empty %}
                <tbody>
                    {% for notification in notifications %}
                        <tr>
                            <td>{{ loop.index }}</td>
                            <td>
                                {% if notification.dateHeureNotif|default(null) is not empty %}
                                    {{ notification.dateHeureNotif|date('d/m/Y H:i:s') }}
                                {% endif %}
                            </td>
                            <td>{{ notification.getNormMotif(notification.motif|default(''))|default('') }}</td>
                            <td>{{ notification.numeroAppel.profile.designation|default('') }}</td>
                            <td>{{ notification.numeroAppel.proprietaire|default('') }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            {% endif %}
        {% endif %}
    </table>
</div>
